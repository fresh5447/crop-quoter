var React = require('react');

var CompareLoss = React.createClass({
  render: function() {
    return (
      <div className="container">
        <h3> Now they are comparing a loss or something </h3>
      </div>
      )
  }
});

module.exports = CompareLoss;